.new-field 
  %h2 Sign up
  = form_for(resource, as: resource_name, url: registration_path(resource_name)) do |f|
    = render "devise/shared/error_messages", resource: resource
    .field
      = f.label :name
      %br/
      = f.text_field :name, autofocus: true, autocomplete: "name", class: "text"
    .field
      = f.label :email
      %br/
      = f.email_field :email, autocomplete: "email", class: "text"
    .field
      = f.label :password
      - if @minimum_password_length
        %em
          (8文字)
      %br/
      = f.password_field :password, autocomplete: "new-password", class: "text"
    .field
      = f.label :password_confirmation
      - if @minimum_password_length
        %em
          (8文字)
      %br/
      = f.password_field :password_confirmation, autocomplete: "new-password", class: "text"
    .actions
      = f.submit "Sign up", class: "submit-button"
  -# autofocus: trueとはページを読み込むと、「オートフォーカス：」の右横の１行テキスト入力欄にフォーカスを当てるので、入力欄でカーソルが点滅する。
  -# autocompleteとは、入力候補として表示される